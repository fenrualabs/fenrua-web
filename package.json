{
  "name": "fenrua-web",
  "version": "1.0.0",
  "private": true,
  "description": "Canonical static Protocol Explorer for fenrua.ai.",
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "packageManager": "npm@11.18.0",
  "scripts": {
    "validate": "node scripts/require-node24.mjs && node --check kernel-status.js && node --check api/chain-progress.js && node scripts/check-static-links.mjs",
    "deploy:production:node24": "node scripts/require-node24.mjs && npx --yes vercel@latest deploy --prod --yes --project fenrua-commons",
    "deploy:prodction:node24": "npm run deploy:production:node24"
  }
}
