# Wrapper testing

This document records the repository-scoped GitHub App publishing test.

The test branch is pushed by exact commit SHA without storing a user credential, and the pull request is opened with a separate, short-lived installation token.
