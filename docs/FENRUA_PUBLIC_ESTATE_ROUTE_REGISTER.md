# Fenrua Public Estate Route Register

Status: implemented

## Current Canonical

`/`, `/architecture/`, `/kernel/`, `/utilities/`, `/research/`, `/research/pn521-cross-limb-borrow/`, `/research/toolchain-evidence-lock/`, `/research/read-only-chain-observation/`, `/verify/`, `/developers/`, `/toolchain/`, `/evidence/`, `/status/`

## Legacy Archive

`/nexus/`, `/fenswap/`, `/fenpresale/`, `/wallet/`, `/support/`, `/legal/`, `/privacy/`, `/terms/`, `/accessibility/`, `/security/`

Legacy archive pages are labelled `LEGACY / SUPERSEDED`, noindexed, excluded from sitemap, and link to the current Layer 0 overview. No final legal policy language is created in this engineering task.
